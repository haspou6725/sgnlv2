Copilot, generate ALL files listed in COPILOT_BUILD_SPEC.md.

Start with:
1) Create full folder structure
2) Create empty files
3) For each file, fully implement real production code (no placeholders)
4) Use async architecture
5) Use real exchange endpoints (LBank, Binance, MEXC, Bybit)
6) Generate docker-compose.yml
7) Generate Dockerfile
8) Generate requirements.txt
9) Generate .env.example
10) Generate README with run instructions
11) Implement all modules exactly as described
12) No mock data, only real logic
13) After finishing generating the first batch of files, continue automatically until entire system is complete.
